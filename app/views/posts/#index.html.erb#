<h1>Mini blog</h1>

<hr>

<ul>
    <% @posts.each do |post| %>
	<li>
	    <p><%= post.title %></p>
	    <p><%= link_to '詳細', post %></p>
	    <p><%= post.text %></p>
	    <p>いいね：<%= post.good_num %>コメント数：投稿日時：<%= post.created_at %></p>
	</li>
    <% end %>
</table>

<%= link_to '新しい投稿', new_post_path %>
